# Optional dependency for embedding-based semantic matching.
# Not included by default because sentence-transformers adds a large
# PyTorch dependency and is not suitable for the default Render deployment.
-r requirements.txt
sentence-transformers==6.0.0
